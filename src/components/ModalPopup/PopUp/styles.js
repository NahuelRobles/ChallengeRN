/** @format */

import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  imageStyle: {
    width: 200,
    height: 200,
    marginTop: 20,
    borderRadius: 10,
    opacity: 0.3,
  },
  textStyle: {
    width: 200,
    textAlign: 'center',
    marginTop: 35,
    fontWeight: 'bold',
  },
  playBtn: {
    position: 'absolute',
    top: 90,
  },
});
